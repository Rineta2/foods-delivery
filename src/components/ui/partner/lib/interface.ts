export interface PartnerType {
  id: string;
  createdAt: string;
  category: string;
  imageUrl: string;
  isActive: boolean;
  buttonText: string;
  description: string;
  link: string;
  text: string;
  title: string;
}
